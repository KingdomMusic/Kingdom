<div id="artist">
<h2>アーティスト編集画面</h2>
<%= form_for([:admin, @artist]) do |f| %>
<h4>アーティスト名</h4>
<%= f.text_field :name, class: "form-control" %><br>
<%= f.submit "更新", class: "form-create" %>
<% end %>
</div>

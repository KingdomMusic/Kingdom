<div id="artist">
<h2>アーティスト登録画面</h2>
<%= form_for([:admin, @artist]) do |f| %>
<h4>アーティスト名</h4>
<%= f.text_field :name, class: "form-control", placeholder: "ラベル名を入力" %><br>
<%= f.submit "投稿", class: "form-create" %>
<% end %>
</div>


<div id="genre">
<h2>ジャンル編集画面</h2>
<%= form_for([:admin, @genre]) do |f| %>
<h4>ジャンル名</h4>
<%= f.text_field :name, class: "form-control" %><br>
<%= f.submit "更新", class: "form-create" %>
<% end %>
</div>

